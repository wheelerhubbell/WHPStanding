# WHP Standing

Wheeler Hubbell Publishing's capability is machine-verifiable standing under explicit authority and bounds. A WHP Standing Evaluation checks exact COPY and identity-preserving COMPOSE over admitted, signed structured provenance under the applicable WHP Standing Profile. A favorable assessment may produce a WHP Standing Mark. It is not an arbitrary-world truth oracle, legal-authority grant, or evidence of unobserved execution.

Start from the signed Mark's discovery carrier or the canonical deployed service's `/.well-known/whp-standing.json`. Follow `/v1/contract` for the precise schema, admitted authorities, HTTP proof, x402 payment requirement and mandatory exact-quote nonce binding. Generic random-nonce x402 buyers are not silently accepted. Only the buyer's own owner-authorized wallet may authorize payment. Validation precedes payment work; paying cannot manufacture authority or a favorable finding.

A Mark's content-addressed independent verifier must execute in a least-privilege sandbox. Admit the root pin using the buyer's actual trust policy; an embedded key cannot appoint itself. Obtain fresh signed registry status. TEST is never LIVE. Use authenticated retrieval of the original bytes without another charge. Do not publicly upload buyer source objects, private keys, wallet credentials or production secrets.

Repository state, tests, live services, directory acceptance and outside sale evidence are distinct. Use DESIGNED, IMPLEMENTED, VERIFIED, DEPLOYED, REGISTERED, LIVE, SOLD, ISSUED and PROPAGATION-PROVEN only for the evidence each requires. The exact original candidate remains preserved in WHP-Standing-v1-candidate-1.zip and the recovery identity manifest. Do not replace it with another product.
