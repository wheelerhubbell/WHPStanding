name: Historical discovery bridge (disabled during public v1 transition)
on: workflow_dispatch
permissions:
  contents: read
jobs:
  historical:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Candidate development workflow preserved in history.'
