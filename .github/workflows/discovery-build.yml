name: Historical assembly disabled — first public v1
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  refuse:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Historical candidate assembly is development provenance only. It cannot overwrite the public v1 contract."
          exit 1
