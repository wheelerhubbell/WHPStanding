name: Historical propagation promotion (disabled during public v1 transition)
on: workflow_dispatch
permissions:
  contents: read
jobs:
  historical:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Candidate extension preserved in history; first-public-v1 proof replaces this promotion gate.'
